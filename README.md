# resume-ranker

<hr>

## How to Use?

Install all the dependencies with:

```bash
./install.sh
```

Run the Gradio UI with:

```bash
gradio app.py
```

Or run it from with:

```bash
python demo.py
```
